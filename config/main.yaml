defaults:
 - backbone: ncdeformer
 - benchmark: uwgl
 - paradigm: supervised
 - _self_

trainer_args:
  max_epochs: -1
  devices: 1

emb_dim: 16
hidden_dim: 128
learning_rate: 1e-3
batch_size: 32
patience: 10
seed: 0

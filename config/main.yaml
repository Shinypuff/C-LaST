defaults:
 - backbone: gru
 - benchmark: churn
 - paradigm: supervised
 - _self_

trainer_args:
  max_epochs: -1

emb_dim: 16
hidden_dim: 128
learning_rate: 1e-3
batch_size: 32
patience: 5
experiment: test
seed: 0
